# Pinned toolchain: the host toolchain is a convenience for editor feedback,
# never a build dependency. The container builds with this same channel, so
# the two cannot disagree.
[toolchain]
channel = "1.97.1"
components = ["rustfmt", "clippy"]
profile = "minimal"
